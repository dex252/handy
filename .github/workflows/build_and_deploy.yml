name: CI/CD

env:
  debug: "Debug"
  release: "Release"
  version: "${{ github.sha }}"
  
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Init
        run: echo "Start in ${{ env.debug }} mode. Version ${{env.version}}."
        
      - name: Git clone repo
        uses: actions/checkout@v2
       
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.
          
#   deploy:
#     runs-on: ubuntu-latest
#     needs: [build]
#     steps:
#       - uses: actions/checkout@v2
      
#       - name: Run a one-line script
#         run: echo Hello, world!
        
#       - name: Run a multi-line script
#         run: |
#           echo Add other actions to build,
#           echo test, and deploy your project.
